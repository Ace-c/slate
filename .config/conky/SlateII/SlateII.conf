
conky.config = {
-- Size and Position settings --
  alignment = 'middle_middle',
  gap_x = 0,
  gap_y = 0,
  maximum_width = 900,
  minimum_height = 230,
  minimum_width = 800,
 
-- Text settings --
  use_xft = true,
  override_utf8_locale = true,
  font = 'Roboto:size=9',
 
-- Color Settings --
  default_color = 'white',
  default_outline_color = 'white',
  default_shade_color = 'white',
  color1 = '#bfc7d6',
  color2 = '#8a95a8',
  color3 = '#d5dae3',
 
-- Window Settings --
  background = false,
  border_width = 1,
  draw_blended = false,
  draw_borders = false,
  draw_graph_borders = false,
  draw_outline = false,
  draw_shades = false,
  own_window = true,
  own_window_argb_value = 0,
  own_window_class = 'Conky',
  own_window_argb_visual = true,
  own_window_type = 'desktop',
  own_window_transparent = true,
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
  stippled_borders = 0,
 
-- Others --
  cpu_avg_samples = 2,
  net_avg_samples = 2,
  double_buffer = true,
  out_to_console = false,
  out_to_stderr = false,
  extra_newline = false,
  update_interval = 1,
  uppercase = false,
  use_spacer = 'none',
  show_graph_scale = false,
  show_graph_range = false,
}
 
conky.text = [[
${image ~/.config/conky/SlateII/res/line.png -s 130x5 -p 335,83}\
${alignc}${voffset 55}${color}${font Big John:size=15}${time %I:%M %p}${font}
${alignc}${voffset 30}${color}${font Monoton:size=50}${time %A,  %d %B}
#${alignc}${voffset -60}${color2}${font JetBrains Mono:size=12}RAM:${memperc}%  CPU:${cpu cpu0}%  TEMP:${hwmon 0 temp 1}°C${font}
${alignc}${voffset -60}${color2}${font JetBrains Mono:size=12}RAM:${memperc}%  CPU:${cpu cpu0}%${font}
]]
